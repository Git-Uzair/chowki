# `@chowki/core` (Node.js / TypeScript SDK)

Reserved for `@chowki/core`, scheduled as roadmap Phase 3 (`docs/plans/00-roadmap.md`). The normative cross-SDK contract it must match lives in `docs/research/07-cross-sdk-parity.md` and `spec/v1/`.
